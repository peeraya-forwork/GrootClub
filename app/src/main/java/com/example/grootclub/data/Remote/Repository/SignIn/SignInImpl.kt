package com.example.grootclub.data.Remote.Repository.SignIn

import com.example.grootclub.data.Remote.Service
import com.example.grootclub.data.Request.SignIn.ReqSignIn
import okhttp3.ResponseBody
import retrofit2.Response

//class SignInImpl(private val service: Service) : SignInRepository {
//    override suspend fun postSignIn(reqSignIn: ReqSignIn): Response<ResponseBody> {
//        // ทำการเรียกใช้ Retrofit service ที่ได้รับมา
//        return service.signIn(reqSignIn)
//    }
//}
